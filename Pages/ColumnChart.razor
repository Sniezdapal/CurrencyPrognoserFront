@using Models
<RadzenCheckBox @bind-Value=isSmooth></RadzenCheckBox>
<RadzenChart>
    @foreach (var dataSet in Data)
        {
            <RadzenColumnSeries Data="@dataSet.Items" CategoryProperty="Date" ValueProperty="CurrencyRate"
                Title="@dataSet.CurrencyName">
                <RadzenMarkers MarkerType="MarkerType.Circle" />
            </RadzenColumnSeries>
        }
        </RadzenChart>


        @code {
    [Parameter]
    public List<DataSet> Data { get; set; }
    bool isSmooth;
}
