<div>
    <div class="m-3 p-2">
        Smooth Chart
        <RadzenSwitch @bind-Value=isSmooth Style="display: flex"></RadzenSwitch>
    </div>
    <RadzenChart>
        @foreach (var dataSet in Data)
        {
            <RadzenLineSeries Data="@dataSet.Value" CategoryProperty="Date" ValueProperty="CurrencyRate"
            Title="@dataSet.Key" Smooth="@isSmooth">
                <RadzenMarkers MarkerType="MarkerType.Circle" />
            </RadzenLineSeries>
        }
    </RadzenChart>
</div>


@code {
    [Parameter]
    public DataSet Data { get; set; }
    bool isSmooth;
}
